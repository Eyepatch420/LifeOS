# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Required noise-suppression for Freezed union classes.
    invalid_annotation_target: ignore
  # NOTE: custom_lint + riverpod_lint are intentionally NOT enabled here.
  # Current custom_lint/riverpod_lint releases pin conflicting transitive
  # analyzer/freezed_annotation constraints against this project's Flutter
  # SDK's bundled analyzer (7.6.0) and freezed_annotation ^2.4.4 — pulling
  # them in breaks the build_runner build script entirely (see
  # docs/future_work.md). Revisit once a compatible release lands.

linter:
  rules:
    # Correctness / safety
    - always_use_package_imports
    - avoid_dynamic_calls
    - avoid_type_to_string
    - cancel_subscriptions
    - close_sinks
    - no_leading_underscores_for_local_identifiers
    - unnecessary_await_in_return
    - use_build_context_synchronously

    # Style / consistency
    - prefer_single_quotes
    - sort_constructors_first
    - sort_unnamed_constructors_first
    - unnecessary_lambdas
    - unnecessary_parenthesis

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
