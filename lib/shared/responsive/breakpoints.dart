/// Width (logical pixels) above which layouts reflow into their wider
/// tablet arrangement. Phone is the primary breakpoint; this is the single
/// threshold every responsive widget in the app checks against.
const double kTabletBreakpoint = 700;
