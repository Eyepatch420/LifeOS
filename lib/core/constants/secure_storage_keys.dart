/// flutter_secure_storage key names — reserved for values sensitive enough
/// that a plaintext prefs file shouldn't hold them.
abstract final class SecureStorageKeys {
  static const String biometricLockEnabled = 'biometric_lock_enabled';
}
